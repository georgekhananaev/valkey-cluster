# ───── Minimal Valkey config for a single-host cluster ─────
bind 0.0.0.0
protected-mode no

cluster-enabled yes
cluster-node-timeout 5000
# Each instance overrides cluster-config-file in the launch script
appendonly yes

loglevel notice
